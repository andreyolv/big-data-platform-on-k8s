apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helm-dashboard
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: helm-dashboard
      version: 0.1.3
      sourceRef:
        kind: HelmRepository
        name: helm-dashboard
        namespace: flux-system
      interval: 5m
